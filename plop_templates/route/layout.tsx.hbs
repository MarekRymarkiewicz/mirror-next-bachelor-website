import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "{{name}}",
  description: "{{name}} page.",
};

const {{name}}Layout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default {{name}}Layout;
